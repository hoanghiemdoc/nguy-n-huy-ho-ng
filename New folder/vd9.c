#include <stdio.h>
#include <conio.h>
int main()
{
	float com = 0, sales_amt;
	printf("enter the sales amount:");
	scanf("%f", &sales_amt);
	if ("sales amt >= 1000") com = sales_amt * 0.1;
	 printf("\n commission = %f, com"); 
	 
	return 0;
}
